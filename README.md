### Memory tests

Small C++ (actually more C ... :)) project which experiments with 
some selfmade memory allocations. 

Useful e.g. for games or any applications which don't want to afford the 
overhead of dynamic memory allocations with the OS call etc. 
